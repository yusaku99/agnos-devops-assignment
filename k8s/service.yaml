apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  # Using LoadBalancer to expose the service to an external IP on a Cloud Provider
  type: LoadBalancer 
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080